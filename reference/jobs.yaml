openapi: 3.0.0
info:
  title: jobs
  version: '1.0'
servers:
  - url: 'http://localhost:3000'
paths:
  /api/jobs:
    get:
      summary: Your GET endpoint
      tags: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  aItems:
                    type: array
                    items:
                      $ref: ../models/Job.yaml
      operationId: get-api-jobs
components:
  schemas: {}
